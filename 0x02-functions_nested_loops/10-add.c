#include<stdio.h>
#include"main.h"
/**
 * add-adds two integers and returns the result
 * @x:the first number to be added
 * @y:the scound number to be added
 * Return: sum-the sumation of the two numbers
 */
int add(int x, int y)
{
int sum;

sum = x + y;
return (sum);
}

