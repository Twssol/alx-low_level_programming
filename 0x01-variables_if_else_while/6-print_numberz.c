#include <stdio.h>
/**
 * main- Print digits using putchar function.
 * Return: 0 (success)
 */
int main(void)
{
	int digit;

	for (digit = 48; digit <= 57; digit++)
	putchar (digit);
	putchar ('\n');
	return (0);
}
