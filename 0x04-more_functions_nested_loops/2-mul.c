#include"main.h"
/**
 * mul-multiplies two integers
 * @a: The first intiger
 * @b: The scond integer
 * Return: (mul) the result
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
